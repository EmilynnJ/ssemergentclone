fastapi>=0.110.1
uvicorn>=0.25.0
asyncpg>=0.29.0
python-dotenv>=1.0.1
python-jose[cryptography]>=3.3.0
# Explicitly including [cryptography] as per original plan, cryptography is also listed separately
cryptography>=42.0.8
passlib[bcrypt]>=1.7.4
# Explicitly including [bcrypt] as per original plan
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1 # pyjwt is a dependency of python-jose but good to list if directly used
stripe>=8.0.0
websockets>=12.0
tzdata>=2024.2 # For timezone support, often good to have
python-multipart>=0.0.9 # For form data, if any part of API uses it
requests>=2.31.0 # Often useful, and stripe SDK might use it. Keep for now.
# Removed: boto3, requests-oauthlib, pytest, black, isort, flake8, mypy, pandas, numpy, jq, typer, psycopg2-binary
